# ── Python ────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# ── Virtual environment (HUGE — never send to Docker) ─────────────
.venv/
venv/
env/

# ── MLflow / DVC (not needed in API image) ────────────────────────
mlruns/
mlflow.db
mlflow.db-shm
mlflow.db-wal
.dvc/
*.dvc

# ── Models (mounted at runtime, not baked in) ─────────────────────
models/

# ── Data ──────────────────────────────────────────────────────────
data/

# ── Training artifacts ────────────────────────────────────────────
catboost_info/
outputs/
.hydra/
logs/
notebooks/

# ── Git ───────────────────────────────────────────────────────────
.git/
.gitignore

# ── Secrets ───────────────────────────────────────────────────────
.env
.env.local

# ── Reports ───────────────────────────────────────────────────────
reports/

# ── Test files ────────────────────────────────────────────────────
tests/

# ── Lock files (using requirements-api.txt instead) ───────────────
uv.lock

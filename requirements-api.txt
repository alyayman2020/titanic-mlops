# API-only dependencies — much smaller than full training environment
# No PyTorch, no DVC, no Hydra, no Optuna

# ── Web framework ─────────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pydantic==2.6.4

# ── ML inference (model loading only) ────────────────────────────
scikit-learn==1.4.2
numpy==1.26.4
pandas==2.2.1
joblib==1.3.2

# ── Gradient boosting (for loading tuned pkl models) ──────────────
xgboost==2.0.3
lightgbm==4.3.0
catboost==1.2.5

# ── MLflow + DagsHub (load from registry) ────────────────────────
mlflow==2.12.2
dagshub==0.3.25
python-dotenv==1.0.1

# ── Utilities ─────────────────────────────────────────────────────
loguru==0.7.2
requests==2.31.0
